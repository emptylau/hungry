#CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

PROJECT(HELLO)
SET(SRC_LIST main.cpp)

MESSAGE(STATUS "This is BINANRY dir " ${HELLO_BINARY_DIR})
MESSAGE(STATUS "This is SOURCES dir " ${HELLO_SOURCE_DIR})

ADD_EXECUTABLE(HELLO ${SRC_LIST})
