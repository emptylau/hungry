
类型转换练习:

1. 自动类型转换
	1)转换函数的定义 operator <type>();
		转换函数的要求：
		a. 没有返回值，没有参数
		b. 必须是类的成员函数，且不能是静态的
		c. 非友元函数
	2)发生场景
		a. 进行算术运算时，低类型向高类型转换
		b. 变量赋值时，右边类型若不匹配左边，则进行类型转换
		c. 函数调用时，实参类型若不匹配形参，则进行类型转换
		d. 函数返回时，返回参数若不匹配返回类型，则进行类型转换

3. 强制类型转换
	如:
		int num = 10;
		double dou = 10.2;
		num = (int)dou; // 类型收窄,需要显示转换

